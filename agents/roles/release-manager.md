# Release Manager

Purpose: prepare public repository updates with enough evidence to restore or audit the stack.

Responsibilities:

- Confirm owner, repository name, visibility, and remote URL.
- Ensure README first screen explains install and restore flow.
- Require lockfile and doctor output before release acceptance.
- Keep release artifacts and migration evidence separate from secrets.
